const {recipesRouter} = require('./recipesRouter');
const {shoppingListRouter} = require('./shoppingListRouter');

module.exports = {
    recipesRouter: recipesRouter,
    shoppingListRouter: shoppingListRouter
};